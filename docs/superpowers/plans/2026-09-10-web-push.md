# Web-Push-Benachrichtigungen Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Server prüft morgens + pre-game die Ligen jedes Push-Abos und schickt bei Lineup-Problemen bzw. Waiver-Chancen genau eine Web-Push-Nachricht pro Lauf und Gerät (Android-PWA).

**Architecture:** `vite-plugin-pwa` (injectManifest) liefert den Service Worker; neue `/api/push/*`-Routen in `apiRoutes.js` verwalten VAPID-Key + Subscriptions (JSON-Datei, Muster wie Scores); `node-cron`-Scheduler (nur mit `SDH_SCHEDULER=1`) baut Nachrichten aus wiederverwendeter Client-Logik (`bestLineup`, `buildAllTeamsRows`, `pickupRanking` – alles ESM ohne Browser-APIs) und versendet via `web-push`.

**Tech Stack:** `web-push`, `node-cron`, `vite-plugin-pwa` (dev), VAPID, vorhandene `rankings.js`-Scraper, Node 24 (global `fetch`).

**Spec:** `docs/superpowers/specs/2026-09-10-web-push-notifications-design.md`

## Global Constraints

- UI-Texte, Kommentare und neue Strings sind **deutsch** (Repo-Konvention).
- Endpoint-Änderungen passieren **nur in `src/server/apiRoutes.js`** (dünne Entrypoints `index.js`/`prod.js` bleiben dünn – einziger Zusatz dort: Scheduler-Start, je eine Zeile).
- `npm test` (Vitest) muss nach jedem Task grün sein; neue Server-Tests liegen neben dem Code (`src/server/*.test.js`), Client-Tests neben der Komponente.
- Kein Linter im Repo – keine Lint-Schritte einplanen.
- TDD: Test zuerst schreiben, Fail verifizieren, minimal implementieren.
- Cron-Zeiten (Europe/Berlin): morgens `0 8 * * *`, pre-game Fr `30 0 * * 5` (vor TNF ~02:15 MEZ), So `30 17 * * 0` (vor 19:00-Spielen). Als benannte Konstanten, keine Magic Strings.

---

## File Map

| Datei | Verantwortung |
|---|---|
| `src/server/push.js` (neu) | Subscription-Storage (JSON, tmp+rename wie Scores) + `sendPush`-Wrapper um `web-push`, VAPID-Config aus Env |
| `src/server/push.test.js` (neu) | Storage-Tests (add/remove/lesen, korrupte Datei), Payload-Validierung |
| `src/server/apiRoutes.js` (edit) | 3 Routen: `GET /api/push/vapid-key`, `POST /api/push/subscribe`, `POST /api/push/unsubscribe` |
| `src/server/apiRoutes.test.js` (edit) | Routen-Liste um die 3 Push-Routen erweitern |
| `src/server/lineupCheck.js` (neu) | Reine Check-Engine: aus Sleeper-Daten → `{ warnings, pickups }` + `composeMessage()` (max. 1 Nachricht) |
| `src/server/lineupCheck.test.js` (neu) | Engine mit gemockten Fetchern, Message-Komposition, Leer-Fälle |
| `src/server/scheduler.js` (neu) | Cron-Verdrahtung, `SDH_SCHEDULER`-Guard, `runCheck({type, deps})` mit injizierbaren Deps |
| `src/server/scheduler.test.js` (neu) | `runCheck` mit Mocks (kein echter Cron, keine echten Requests) |
| `src/server/index.js`, `src/server/prod.js` (edit, je 1 Zeile) | `startScheduler()` aufrufen |
| `src/sw.js` (neu) | Service Worker: Precache + `push`-Handler + `notificationclick` → `/lineup` |
| `vite.config.js` (edit) | `VitePWA({ strategies: 'injectManifest', ... })` |
| `src/services/pushClient.js` (neu) | `subscribePush(username)`, `unsubscribePush()`, `getPushState()` |
| `src/components/PushOptIn.jsx` (neu) + Test | Glocke-Button mit deutschem Opt-in-Dialog, auf Lineup-Seite im `an-head` |
| `src/pages/LineupPage.jsx` (edit) | `<PushOptIn />` im Header rendern |

---

### Task 1: Dependencies + VAPID-Keys

**Files:**
- Modify: `package.json` (via npm, nicht per Hand)

**Interfaces:**
- Consumes: nichts
- Produces: `web-push`, `node-cron` (deps), `vite-plugin-pwa` (devDep) importierbar; VAPID-Keypaar im Server-Env

- [ ] **Step 1: Pakete installieren**

```bash
npm install web-push node-cron
npm install -D vite-plugin-pwa
```

- [ ] **Step 2: Verifizieren dass Imports auflösen**

```bash
node -e "import('web-push').then(() => console.log('web-push ok'))"
node -e "import('node-cron').then(() => console.log('node-cron ok'))"
```

Expected: beide Zeilen `ok`, Exit 0.

- [ ] **Step 3: VAPID-Keys generieren und dokumentieren**

```bash
npx web-push generate-vapid-keys
```

Expected: gibt `Public Key` + `Private Key` aus. Keys gehören auf den Server als `SDH_VAPID_PUBLIC_KEY`, `SDH_VAPID_PRIVATE_KEY` plus `SDH_VAPID_SUBJECT=mailto:<deine-mail>` – **nicht** committen, nur lokal + Prod-Env setzen. Public Key wird später per `GET /api/push/vapid-key` verteilt.

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json
git commit -m "feat(push): deps web-push, node-cron, vite-plugin-pwa"
```

---

### Task 2: Server Push-Storage + Routen

**Files:**
- Create: `src/server/push.js`
- Create: `src/server/push.test.js`
- Modify: `src/server/apiRoutes.js` (nur: Import + 3 Routen in `registerApiRoutes`)
- Modify: `src/server/apiRoutes.test.js` (Routen-Liste erweitern)

**Interfaces:**
- Consumes: `SCORES_FILE`-Muster aus `apiRoutes.js:339` (Env-Override + tmpdir-Fallback), tmp+rename aus `apiRoutes.js:411-416`
- Produces: `readSubs(file)`, `addSub({subscription, sleeperUsername}, file)`, `removeSub(endpoint, file)`, `getVapidConfig()`, `sendPush(sub, payload)`; Routen `GET /api/push/vapid-key`, `POST /api/push/subscribe`, `POST /api/push/unsubscribe`

Subscription-Form: `{ endpoint, keys: { p256dh, auth }, sleeperUsername, createdAt }`, Datei enthält Array (max. 500 Einträge, ein Eintrag je `endpoint`).

- [ ] **Step 1: Failing Test für Storage schreiben** (`src/server/push.test.js`)

```js
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import fs from 'fs'
import os from 'os'
import path from 'path'
import { readSubs, addSub, removeSub } from './push.js'

let dir
let file
beforeEach(() => { dir = fs.mkdtempSync(path.join(os.tmpdir(), 'sdh-push-test-')); file = path.join(dir, 'push.json') })
afterEach(() => { fs.rmSync(dir, { recursive: true, force: true }) })

const sub = () => ({ endpoint: 'https://push.example/e1', keys: { p256dh: 'p', auth: 'a' }, sleeperUsername: 'Zmash' })

describe('push storage', () => {
  it('liest leeres Array wenn Datei fehlt', () => {
    expect(readSubs(file)).toEqual([])
  })

  it('speichert ein Abo mit Username und liest es zurück', () => {
    addSub({ subscription: sub(), sleeperUsername: 'Zmash' }, file)
    const all = readSubs(file)
    expect(all).toHaveLength(1)
    expect(all[0].sleeperUsername).toBe('Zmash')
    expect(all[0].endpoint).toBe('https://push.example/e1')
  })

  it('überschreibt statt zu doppeln bei gleichem endpoint', () => {
    addSub({ subscription: sub(), sleeperUsername: 'Alt' }, file)
    addSub({ subscription: sub(), sleeperUsername: 'Neu' }, file)
    const all = readSubs(file)
    expect(all).toHaveLength(1)
    expect(all[0].sleeperUsername).toBe('Neu')
  })

  it('entfernt ein Abo per endpoint', () => {
    addSub({ subscription: sub(), sleeperUsername: 'Zmash' }, file)
    removeSub('https://push.example/e1', file)
    expect(readSubs(file)).toEqual([])
  })

  it('wirft bei ungültigem Abo (ohne endpoint)', () => {
    expect(() => addSub({ subscription: { keys: {} }, sleeperUsername: 'x' }, file)).toThrow()
  })
})
```

- [ ] **Step 2: Test laufen lassen, Fail verifizieren**

Run: `npm test -- src/server/push.test.js`
Expected: FAIL mit "Failed to resolve import ./push.js".

- [ ] **Step 3: Minimale Implementierung** (`src/server/push.js`)

```js
import fs from 'fs'
import os from 'os'
import path from 'path'
import webpush from 'web-push'

export const PUSH_FILE = process.env.SDH_PUSH_FILE || path.join(os.tmpdir(), 'sdh-push.json')
const MAX_SUBS = 500

export function getVapidConfig() {
  const publicKey = process.env.SDH_VAPID_PUBLIC_KEY || null
  const privateKey = process.env.SDH_VAPID_PRIVATE_KEY || null
  const subject = process.env.SDH_VAPID_SUBJECT || null
  if (!publicKey || !privateKey || !subject) return null
  return { publicKey, privateKey, subject }
}

export function readSubs(file = PUSH_FILE) {
  try {
    const raw = fs.readFileSync(file, 'utf8')
    const arr = JSON.parse(raw)
    return Array.isArray(arr) ? arr : []
  } catch { return [] }
}

function writeSubs(all, file) {
  fs.mkdirSync(path.dirname(file), { recursive: true })
  const tmp = `${file}.${process.pid}.tmp`
  fs.writeFileSync(tmp, JSON.stringify(all.slice(0, MAX_SUBS)))
  fs.renameSync(tmp, file)
}

export function addSub({ subscription, sleeperUsername }, file = PUSH_FILE) {
  const endpoint = subscription?.endpoint
  const keys = subscription?.keys
  const user = String(sleeperUsername || '').trim()
  if (!endpoint || !keys?.p256dh || !keys?.auth || !user) {
    throw new Error('Ungültige Subscription (endpoint/keys/username fehlen)')
  }
  const all = readSubs(file).filter((s) => s.endpoint !== endpoint)
  all.push({ endpoint, keys: { p256dh: keys.p256dh, auth: keys.auth }, sleeperUsername: user, createdAt: new Date().toISOString() })
  writeSubs(all, file)
  return true
}

export function removeSub(endpoint, file = PUSH_FILE) {
  writeSubs(readSubs(file).filter((s) => s.endpoint !== String(endpoint)), file)
  return true
}

export async function sendPush(sub, payload) {
  const vapid = getVapidConfig()
  if (!vapid) throw new Error('VAPID nicht konfiguriert')
  webpush.setVapidDetails(vapid.subject, vapid.publicKey, vapid.privateKey)
  return webpush.sendNotification({ endpoint: sub.endpoint, keys: sub.keys }, JSON.stringify(payload))
}
```

- [ ] **Step 4: Tests grün verifizieren**

Run: `npm test -- src/server/push.test.js`
Expected: PASS (5 Tests).

- [ ] **Step 5: Routen in `apiRoutes.js` ergänzen** (Import oben + in `registerApiRoutes`, neben `/api/score`):

```js
import { readSubs as readPushSubs, addSub as addPushSub, removeSub as removePushSub, getVapidConfig } from './push.js'

app.get('/api/push/vapid-key', (req, res) => {
  const vapid = getVapidConfig()
  if (!vapid) return res.status(503).json({ error: 'Push nicht konfiguriert' })
  res.json({ publicKey: vapid.publicKey })
})

app.post('/api/push/subscribe', (req, res) => {
  try {
    addPushSub({ subscription: req.body?.subscription, sleeperUsername: req.body?.sleeperUsername })
    res.json({ ok: true })
  } catch (e) {
    res.status(400).json({ error: e.message })
  }
})

app.post('/api/push/unsubscribe', (req, res) => {
  removePushSub(req.body?.endpoint)
  res.json({ ok: true })
})
```

- [ ] **Step 6: Routen-Assertion in `apiRoutes.test.js:29-36` erweitern** (zwei Einträge in die `for`-Liste):

```js
'GET /api/push/vapid-key', 'POST /api/push/subscribe', 'POST /api/push/unsubscribe',
```

- [ ] **Step 7: Alle Server-Tests grün**

Run: `npm test -- src/server/push.test.js src/server/apiRoutes.test.js`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add src/server/push.js src/server/push.test.js src/server/apiRoutes.js src/server/apiRoutes.test.js
git commit -m "feat(push): subscription storage + /api/push routes"
```

---

### Task 3: Check-Engine (Warnungen + Nachricht)

**Files:**
- Create: `src/server/lineupCheck.js`
- Create: `src/server/lineupCheck.test.js`

**Interfaces:**
- Consumes: `buildAllTeamsRows` aus `src/services/analysis/allTeamsLineup.js`, `bestLineup` + `matchKey` aus `src/services/analysis/waiverStats.js`, `pickupRanking` + `freeAgents` aus derselben Datei, Sleeper-Fetch via injizierbare Deps (Default: globale `fetch` gegen `https://api.sleeper.app/v1`, Muster aus `src/services/api.js:7-13`)
- Produces: `checkUserLeagues({ username, season, deps })` → `{ warnings: [...], pickups: [...] }`; `composeMessage({ warnings, pickups, type })` → Nachricht `{ title, body, url }` oder `null` (nichts zu melden)

Zeilen-Form: `{ leagueName, playerName, pos, team, severity, reason }`. `type` ist `'morning'` oder `'pregame'`. Regel: genau eine Nachricht oder `null`. Morning enthält Lineup-Top-Warnung + besten Pickup; Pregame nur Lineup.

- [ ] **Step 1: Failing Tests schreiben** (`src/server/lineupCheck.test.js`)

```js
import { describe, it, expect } from 'vitest'
import { composeMessage } from './lineupCheck.js'

const warn = (over = {}) => ({
  leagueName: 'Dynasty', playerName: 'A.J. Brown', pos: 'WR', team: 'NE',
  severity: 'red', reason: 'out', ...over,
})

describe('composeMessage', () => {
  it('gibt null bei leeren Befunden', () => {
    expect(composeMessage({ warnings: [], pickups: [], type: 'morning' })).toBeNull()
  })

  it('meldet die rote Warnung mit Liga und Spieler', () => {
    const msg = composeMessage({ warnings: [warn()], pickups: [], type: 'pregame' })
    expect(msg.url).toBe('/lineup')
    expect(msg.title).toContain('Dynasty')
    expect(msg.body).toContain('A.J. Brown')
  })

  it('morning hängt den besten Pickup an', () => {
    const msg = composeMessage({
      warnings: [warn()],
      pickups: [{ name: 'Jordan Love', pos: 'QB', team: 'GB', leagueName: 'FFL' }],
      type: 'morning',
    })
    expect(msg.body).toContain('Jordan Love')
  })

  it('pregame ignoriert Pickups', () => {
    const msg = composeMessage({
      warnings: [warn()],
      pickups: [{ name: 'Jordan Love', pos: 'QB', team: 'GB', leagueName: 'FFL' }],
      type: 'pregame',
    })
    expect(msg.body).not.toContain('Jordan Love')
  })

  it('nur gelbe Warnungen werden auch gemeldet', () => {
    const msg = composeMessage({
      warnings: [warn({ severity: 'yellow', reason: 'better-on-bench', playerName: 'Jordan Addison' })],
      pickups: [], type: 'morning',
    })
    expect(msg).not.toBeNull()
    expect(msg.body).toContain('Jordan Addison')
  })
})
```

- [ ] **Step 2: Fail verifizieren**

Run: `npm test -- src/server/lineupCheck.test.js`
Expected: FAIL (Import fehlt).

- [ ] **Step 3: `composeMessage` + Engine-Gerüst implementieren** (`src/server/lineupCheck.js`)

```js
import { buildAllTeamsRows } from '../services/analysis/allTeamsLineup.js'

const REASON_TEXT = { bye: 'Bye', out: 'Out', suboptimal: 'Bank?', 'better-on-bench': 'Starten?', questionable: 'Fraglich' }

export function composeMessage({ warnings = [], pickups = [], type = 'morning' } = {}) {
  const relevant = warnings.filter((w) => w.severity === 'red' || w.severity === 'yellow')
  if (!relevant.length && !pickups.length) return null
  const order = { red: 0, yellow: 1 }
  const top = [...relevant].sort((a, b) => (order[a.severity] ?? 9) - (order[b.severity] ?? 9))[0]
  let title = 'Lineup prüfen'
  let body = ''
  if (top) {
    title = `${top.severity === 'red' ? 'Problem' : 'Hinweis'} · ${top.leagueName}`
    body = `${top.playerName} (${top.pos}) – ${REASON_TEXT[top.reason] || top.reason}`
  }
  if (type === 'morning' && pickups.length) {
    const p = pickups[0]
    const waiver = `Waiver: ${p.name} (${p.pos}${p.team ? `, ${p.team}` : ''})`
    body = body ? `${body} · ${waiver}` : waiver
    if (!top) title = `Waiver · ${p.leagueName || ''}`.trim()
  }
  if (!body) return null
  return { title, body, url: '/lineup', tag: `sdh-${type}` }
}

// checkUserLeagues folgt in Step 5 (braucht Sleeper-Fetch + Meta-Cache).
export async function checkUserLeagues() {
  throw new Error('noch nicht implementiert (Step 5)')
}
```

Engine-Regel: `checkUserLeagues({ username, season, deps })` lädt je Liga Kader + Starter (Sleeper `/user/{id}/leagues/nfl/{season}`, `/league/{id}/rosters`), baut Spieler-Meta aus datei-cached `/players/nfl` (24h TTL, Slim-Felder wie `src/services/playersMeta.js:12-26`), rechnet je Liga `bestLineup` → `recommendedStarterIds`, flacht alles über `buildAllTeamsRows` zu Warnungen ab und nimmt je Liga Top-3 aus `pickupRanking`. Alle Netzwerk-Zugriffe nur über `deps`-Funktionen (Default: echte Sleeper-Calls), damit Step-5-Tests ohne Netz laufen.

- [ ] **Step 4: Tests grün**

Run: `npm test -- src/server/lineupCheck.test.js`
Expected: PASS (5 Tests).

- [ ] **Step 5: `checkUserLeagues` mit injizierbaren Deps implementieren + Tests**

Tests (selbe Datei, neuer `describe`-Block, keine echten Requests):

```js
import { checkUserLeagues } from './lineupCheck.js'

const meta = {
  1: { full_name: 'Out Spieler', fantasy_positions: ['WR'], team: 'NE', bye_week: 9, injury_status: 'Out' },
  2: { full_name: 'Bank Star', fantasy_positions: ['WR'], team: 'MIN', bye_week: 9, injury_status: null },
}
const deps = () => ({
  fetchLeagues: async () => [{ league_id: 'l1', name: 'Dynasty', roster_positions: ['WR', 'BN', 'BN'] }],
  fetchRosters: async () => [{ owner_id: 'u1', players: ['1', '2'], starters: ['1'] }],
  fetchMeta: async () => meta,
  weekRanks: async () => ({ weeklyById: new Map([['ID:1', 90], ['ID:2', 5]]), flexById: new Map(), sflexById: new Map() }),
  rosPicks: async () => [{ player_id: '9', name: 'Jordan Love', pos: 'QB', team: 'GB', leagueName: 'Dynasty' }],
  resolveUserId: async () => 'u1',
  week: '5',
})

describe('checkUserLeagues', () => {
  it('findet den aufgestellten Out-Spieler als rote Warnung', async () => {
    const { warnings } = await checkUserLeagues({ username: 'Zmash', season: '2026', deps: deps() })
    const out = warnings.find((w) => w.playerName === 'Out Spieler')
    expect(out.severity).toBe('red')
    expect(out.leagueName).toBe('Dynasty')
  })

  it('liefert Pickups aus rosPicks', async () => {
    const { pickups } = await checkUserLeagues({ username: 'Zmash', season: '2026', deps: deps() })
    expect(pickups[0].name).toBe('Jordan Love')
  })

  it('leere Ligen geben leere Befunde', async () => {
    const d = deps()
    d.fetchLeagues = async () => []
    expect(await checkUserLeagues({ username: 'x', season: '2026', deps: d })).toEqual({ warnings: [], pickups: [] })
  })
})
```

Implementierung: Sleeper-Calls als Default-Deps mit globalem `fetch` (Node 24) gegen `https://api.sleeper.app/v1`; Meta-Cache als JSON-Datei neben `PUSH_FILE` (`sdh-players-meta.json`, 24h TTL, Slim-Felder aus `playersMeta.js`); Wochenränge über `src/server/rankings.js` (`fantasyProsPositionUrl:140`, `extractEcrData:189`) mit try/catch-Degradation (leere Maps → Warnungen nur aus Bye/Out, wie Client ohne Rankings).

- [ ] **Step 6: Tests grün**

Run: `npm test -- src/server/lineupCheck.test.js`
Expected: PASS (8 Tests).

- [ ] **Step 7: Commit**

```bash
git add src/server/lineupCheck.js src/server/lineupCheck.test.js
git commit -m "feat(push): check engine + message compose"
```

---

### Task 4: Scheduler-Verdrahtung

**Files:**
- Create: `src/server/scheduler.js`
- Create: `src/server/scheduler.test.js`
- Modify: `src/server/index.js` (+1 Zeile), `src/server/prod.js` (+1 Zeile)

**Interfaces:**
- Consumes: `readSubs`/`removeSub`/`sendPush` aus Task 2, `checkUserLeagues`/`composeMessage` aus Task 3
- Produces: `startScheduler(deps)` (plant 3 Crons, no-op ohne `SDH_SCHEDULER=1`, gibt Stop-Funktionen zurück), `runCheck({ type, deps })` (lädt Abos, prüft je Username, sendet max. 1 Nachricht, löscht tote Abos bei 410/404)

- [ ] **Step 1: Failing Test für `runCheck`** (`src/server/scheduler.test.js`)

```js
import { describe, it, expect, vi } from 'vitest'
import { runCheck } from './scheduler.js'

const sub = { endpoint: 'https://push.example/e1', keys: { p256dh: 'p', auth: 'a' }, sleeperUsername: 'Zmash' }

function deps(over = {}) {
  return {
    loadSubs: async () => [sub],
    checkUserLeagues: async () => ({
      warnings: [{ leagueName: 'Dynasty', playerName: 'A.J. Brown', pos: 'WR', team: 'NE', severity: 'red', reason: 'out' }],
      pickups: [],
    }),
    sendPush: vi.fn(async () => {}),
    removeSub: vi.fn(),
    ...over,
  }
}

describe('runCheck', () => {
  it('sendet genau eine Nachricht bei Warnungen', async () => {
    const d = deps()
    const res = await runCheck({ type: 'pregame', deps: d })
    expect(d.sendPush).toHaveBeenCalledTimes(1)
    expect(res).toEqual({ checked: 1, sent: 1, pruned: 0 })
  })

  it('sendet nichts ohne Befunde', async () => {
    const d = deps({ checkUserLeagues: async () => ({ warnings: [], pickups: [] }) })
    const res = await runCheck({ type: 'morning', deps: d })
    expect(d.sendPush).not.toHaveBeenCalled()
    expect(res.sent).toBe(0)
  })

  it('löscht tote Abos bei 410 und zählt sie', async () => {
    const err = new Error('gone'); err.statusCode = 410
    const d = deps({ sendPush: vi.fn(async () => { throw err }) })
    const res = await runCheck({ type: 'morning', deps: d })
    expect(d.removeSub).toHaveBeenCalledWith('https://push.example/e1')
    expect(res.pruned).toBe(1)
  })

  it('ein fehlerhafter User bricht die anderen nicht ab', async () => {
    const d = deps({
      loadSubs: async () => [sub, { ...sub, endpoint: 'https://push.example/e2' }],
      checkUserLeagues: vi.fn()
        .mockRejectedValueOnce(new Error('Sleeper down'))
        .mockResolvedValueOnce({ warnings: [], pickups: [] }),
    })
    const res = await runCheck({ type: 'morning', deps: d })
    expect(res.checked).toBe(2)
  })
})
```

- [ ] **Step 2: Fail verifizieren**

Run: `npm test -- src/server/scheduler.test.js`
Expected: FAIL (Import fehlt).

- [ ] **Step 3: `scheduler.js` implementieren**

```js
import cron from 'node-cron'
import { readSubs, removeSub as removePushSub, sendPush as sendPushReal } from './push.js'
import { checkUserLeagues as checkUserLeaguesReal, composeMessage } from './lineupCheck.js'

export const CRON_MORNING = '0 8 * * *'
export const CRON_PREGAME_FRI = '30 0 * * 5'
export const CRON_PREGAME_SUN = '30 17 * * 0'
export const CRON_TZ = 'Europe/Berlin'

export async function runCheck({ type, deps = {} }) {
  const {
    loadSubs = readSubs,
    checkUserLeagues = checkUserLeaguesReal,
    sendPush = sendPushReal,
    removeSub = removePushSub,
    season = String(new Date().getFullYear()),
  } = deps
  const subs = await loadSubs()
  let checked = 0
  let sent = 0
  let pruned = 0
  for (const sub of subs) {
    checked += 1
    try {
      const { warnings, pickups } = await checkUserLeagues({ username: sub.sleeperUsername, season })
      const msg = composeMessage({ warnings, pickups, type })
      if (!msg) continue
      await sendPush(sub, msg)
      sent += 1
    } catch (e) {
      if (e?.statusCode === 410 || e?.statusCode === 404) {
        try { await removeSub(sub.endpoint) } catch { /* schon weg */ }
        pruned += 1
      } else {
        console.warn(`[scheduler] check failed (${sub.sleeperUsername})`, e?.message || e)
      }
    }
  }
  console.log(`[scheduler] ${type}: checked=${checked} sent=${sent} pruned=${pruned}`)
  return { checked, sent, pruned }
}

export function startScheduler({ schedule = cron.schedule } = {}) {
  if (process.env.SDH_SCHEDULER !== '1') return []
  const jobs = [
    schedule(CRON_MORNING, () => runCheck({ type: 'morning' }).catch((e) => console.warn('[scheduler] morning failed', e)), { timezone: CRON_TZ }),
    schedule(CRON_PREGAME_FRI, () => runCheck({ type: 'pregame' }).catch((e) => console.warn('[scheduler] pregame failed', e)), { timezone: CRON_TZ }),
    schedule(CRON_PREGAME_SUN, () => runCheck({ type: 'pregame' }).catch((e) => console.warn('[scheduler] pregame failed', e)), { timezone: CRON_TZ }),
  ]
  console.log('[scheduler] aktiv (morning + pregame Fr/So, Europe/Berlin)')
  return jobs
}
```

- [ ] **Step 4: Entrypoints verdrahten** (jeweils eine Zeile nach `registerApiRoutes`):

`src/server/index.js` und `src/server/prod.js`:
```js
import { startScheduler } from './scheduler.js'
startScheduler()
```

- [ ] **Step 5: Tests grün**

Run: `npm test -- src/server/scheduler.test.js`
Expected: PASS (4 Tests).

- [ ] **Step 6: Commit**

```bash
git add src/server/scheduler.js src/server/scheduler.test.js src/server/index.js src/server/prod.js
git commit -m "feat(push): scheduler mit SDH_SCHEDULER-guard"
```

---

### Task 5: Service Worker + Build-Integration

**Files:**
- Create: `src/sw.js`
- Modify: `vite.config.js`
- Test: Build-Artefakt (`dist/sw.js` entsteht)

**Interfaces:**
- Consumes: `vite-plugin-pwa` (Task 1)
- Produces: registrierter SW in Prod (`dist/sw.js`), `push`-Event → Notification mit `{ title, body, url }`, `notificationclick` → `/lineup` öffnen/fokussieren

- [ ] **Step 1: `vite.config.js` erweitern**

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      strategies: 'injectManifest',
      srcDir: 'src',
      filename: 'sw.js',
      injectRegister: 'auto',
      manifest: {
        name: 'Sleeper Draft Helper',
        short_name: 'SDH',
        start_url: '/',
        display: 'standalone',
        background_color: '#071026',
        theme_color: '#071026',
        icons: [
          { src: '/android-chrome-192x192.png', sizes: '192x192', type: 'image/png' },
          { src: '/android-chrome-512x512.png', sizes: '512x512', type: 'image/png' },
        ],
      },
    }),
  ],
  server: { /* unverändert */ },
})
```

- [ ] **Step 2: `src/sw.js` schreiben**

```js
import { precacheAndRoute } from 'workbox-precaching'

precacheAndRoute(self.__WB_MANIFEST)

self.addEventListener('push', (event) => {
  let data = {}
  try { data = event.data ? event.data.json() : {} } catch { /* kein JSON */ }
  const title = data.title || 'Sleeper Draft Helper'
  const body = data.body || 'Es gibt Neuigkeiten zu deinen Ligen.'
  const url = data.url || '/lineup'
  event.waitUntil(
    self.registration.showNotification(title, { body, icon: '/android-chrome-192x192.png', badge: '/favicon-32x32.png', tag: data.tag || 'sdh', data: { url } })
  )
})

self.addEventListener('notificationclick', (event) => {
  event.notification.close()
  const url = event.notification?.data?.url || '/lineup'
  event.waitUntil(
    (async () => {
      const wins = await self.clients.matchAll({ type: 'window', includeUncontrolled: true })
      for (const w of wins) {
        if (w.url.includes(new URL(url, self.location.origin).pathname)) { await w.focus(); return }
      }
      await self.clients.openWindow(url)
    })()
  )
})
```

- [ ] **Step 3: Build verifizieren**

Run: `npm run build`
Expected: Exit 0, `dist/sw.js` existiert.

```bash
Test-Path dist/sw.js
```

- [ ] **Step 4: Commit**

```bash
git add vite.config.js src/sw.js
git commit -m "feat(push): service worker + pwa build"
```

---

### Task 6: Opt-in-UI (Client)

**Files:**
- Create: `src/services/pushClient.js`
- Create: `src/components/PushOptIn.jsx`
- Create: `src/components/PushOptIn.test.jsx`
- Modify: `src/pages/LineupPage.jsx` (Import + `<PushOptIn />` im `an-head`)

**Interfaces:**
- Consumes: `GET /api/push/vapid-key`, `POST /api/push/subscribe|unsubscribe` (Task 2); `sleeperUsername` aus `useSessionStore`; `registration.pushManager` des SW aus Task 5
- Produces: `<PushOptIn />` – Glocke-Button: aus → Opt-in (Permission + Subscribe), an → Abbestellen; Status-Texte deutsch

- [ ] **Step 1: `pushClient.js` + Failing Komponententest**

`src/services/pushClient.js`:
```js
async function vapidKey() {
  const res = await fetch('/api/push/vapid-key')
  if (!res.ok) throw new Error('Push ist auf dem Server nicht konfiguriert')
  const { publicKey } = await res.json()
  return publicKey
}

function urlBase64ToUint8Array(base64) {
  const padding = '='.repeat((4 - (base64.length % 4)) % 4)
  const b64 = (base64 + padding).replace(/-/g, '+').replace(/_/g, '/')
  const raw = atob(b64)
  return Uint8Array.from([...raw].map((c) => c.charCodeAt(0)))
}

export async function getPushState() {
  if (!('serviceWorker' in navigator) || !('PushManager' in window)) return 'unsupported'
  if (Notification.permission === 'denied') return 'denied'
  const reg = await navigator.serviceWorker.ready
  const sub = await reg.pushManager.getSubscription()
  return sub ? 'subscribed' : 'unsubscribed'
}

export async function subscribePush(sleeperUsername) {
  const user = String(sleeperUsername || '').trim()
  if (!user) throw new Error('Bitte zuerst Sleeper-Username verbinden')
  const key = await vapidKey()
  const reg = await navigator.serviceWorker.ready
  const sub = await reg.pushManager.subscribe({ userVisibleOnly: true, applicationServerKey: urlBase64ToUint8Array(key) })
  const res = await fetch('/api/push/subscribe', {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ subscription: sub.toJSON(), sleeperUsername: user }),
  })
  if (!res.ok) throw new Error('Anmeldung fehlgeschlagen')
  return true
}

export async function unsubscribePush() {
  const reg = await navigator.serviceWorker.ready
  const sub = await reg.pushManager.getSubscription()
  const endpoint = sub?.endpoint
  if (sub) await sub.unsubscribe()
  if (endpoint) {
    await fetch('/api/push/unsubscribe', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ endpoint }),
    })
  }
  return true
}
```

Test `src/components/PushOptIn.test.jsx` (Mocks für `pushClient`, Store mit Username):
```jsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, fireEvent, waitFor } from '@testing-library/react'

vi.mock('../services/pushClient', () => ({
  getPushState: vi.fn(async () => 'unsubscribed'),
  subscribePush: vi.fn(async () => true),
  unsubscribePush: vi.fn(async () => true),
}))
vi.mock('../stores/useSessionStore', () => ({ useSessionStore: () => ({ sleeperUsername: 'Zmash' }) }))

import PushOptIn from './PushOptIn'
import { subscribePush } from '../services/pushClient'

beforeEach(() => { vi.clearAllMocks() })

describe('PushOptIn', () => {
  it('zeigt Anmelde-Button wenn nicht angemeldet', async () => {
    const { container } = render(<PushOptIn />)
    await waitFor(() => expect(container.querySelector('button').textContent).toMatch(/Benachrichtigungen/i))
  })

  it('meldet mit Sleeper-Username an', async () => {
    const { container } = render(<PushOptIn />)
    await waitFor(() => expect(container.querySelector('button')).toBeTruthy())
    fireEvent.click(container.querySelector('button'))
    await waitFor(() => expect(subscribePush).toHaveBeenCalledWith('Zmash'))
  })
})
```

- [ ] **Step 2: Fail verifizieren**

Run: `npm test -- src/components/PushOptIn.test.jsx`
Expected: FAIL (Import `./PushOptIn` fehlt).

- [ ] **Step 3: `PushOptIn.jsx` implementieren**

```jsx
import { useEffect, useState } from 'react'
import { useSessionStore } from '../stores/useSessionStore'
import { getPushState, subscribePush, unsubscribePush } from '../services/pushClient'
import Icon from './Icon'

export default function PushOptIn() {
  const { sleeperUsername } = useSessionStore()
  const [state, setState] = useState('loading')
  const [error, setError] = useState(null)

  useEffect(() => { getPushState().then(setState).catch(() => setState('unsupported')) }, [])

  async function toggle() {
    setError(null)
    try {
      if (state === 'subscribed') {
        await unsubscribePush()
        setState('unsubscribed')
      } else {
        await subscribePush(sleeperUsername)
        setState('subscribed')
      }
    } catch (e) {
      setError(e.message || 'Fehlgeschlagen')
    }
  }

  if (state === 'loading' || state === 'unsupported') return null
  if (state === 'denied') return <span className="an-head-meta">Push im Browser blockiert</span>
  return (
    <span className="an-head-meta">
      <button type="button" className="btn btn-secondary btn-sm" onClick={toggle} title={state === 'subscribed' ? 'Push abbestellen' : 'Bei Lineup-Problemen + Waiver benachrichtigen'}>
        <Icon name="bell" size={14} /> {state === 'subscribed' ? 'Push an' : 'Benachrichtigungen'}
      </button>
      {error && <span className="muted"> · {error}</span>}
    </span>
  )
}
```

Hinweis: `Icon name="bell"` – existiert der Name nicht, rendert `Icon` Fallback (vgl. andere Namen wie `refresh`, `x`). Falls nötig auf vorhandenen Namen wechseln.

- [ ] **Step 4: In `LineupPage.jsx`-Header einhängen** (neben `an-head-meta`):

```jsx
<span className="an-head-meta">...bestehend...</span>
<PushOptIn />
```

- [ ] **Step 5: Tests grün**

Run: `npm test -- src/components/PushOptIn.test.jsx src/pages/LineupPage.test.jsx`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/services/pushClient.js src/components/PushOptIn.jsx src/components/PushOptIn.test.jsx src/pages/LineupPage.jsx
git commit -m "feat(push): opt-in UI auf der Lineup-Seite"
```

---

### Task 7: End-to-End-Verifikation + Deploy-Notizen

**Files:** keine (Verifikation + Doku im Commit unten nur falls nötig)

- [ ] **Step 1: Volle Test-Suite**

Run: `npm test`
Expected: 0 Failures (bis dahin ca. 830+ Tests inkl. ~20 neuer Push-Tests).

- [ ] **Step 2: Prod-Build mit SW**

Run: `npm run build`
Expected: Exit 0, `dist/sw.js` + `dist/manifest.webmanifest` vorhanden.

- [ ] **Step 3: Lokaler E2E-Test (Handarbeit, localhost = Secure Context)**

```bash
PORT=8080 node src/server/prod.js
```

Dann: Android-Chrome (oder Desktop-Chrome) → `http://<lan-ip>:8080/lineup` → „Benachrichtigungen" → erlauben → Abo entsteht. Trigger manuell z. B. per Node-One-Liner mit `SDH_SCHEDULER=1`-Prozess oder direktem `runCheck`-Aufruf. Erwartung: Notification mit Titel aus `composeMessage`, Klick öffnet `/lineup`.

- [ ] **Step 4: Prod-Deploy-Checkliste** (auf deinem Server setzen, nichts committen):
  - `SDH_VAPID_PUBLIC_KEY`, `SDH_VAPID_PRIVATE_KEY`, `SDH_VAPID_SUBJECT=mailto:…`
  - `SDH_PUSH_FILE` (persistentes Volume, z. B. neben Scores), `SDH_SCHEDULER=1`
  - HTTPS aktiv (sonst kein Push)
  - Nach Deploy: einmal Opt-in antippen + Testlauf abwarten (morgens 8:00 oder Pre-Game)

---

## Self-Review

- **Spec-Abdeckung:** Ziele 1–3 → Task 3+4 (Checks, Komposition, Anti-Spam-Regeln in `composeMessage`/`runCheck`); Architektur-Block → Tasks 1/2/5/6; Fehlerfälle → Task 4 (`pruned`, try/catch je User) + Task 3 (Degradation); Testing → Tasks mit echten Test-Code-Blöcken; offene Plan-Punkte (Zeiten, Opt-in-Ort, Dynasty-Waiver) sind oben entschieden (Fr 00:30 / So 17:30, Lineup-Header, Top-3 auch Dynasty).
- **Platzhalter:** keine TBD/TODOs; alle Code-Blöcke sind kopierbar; Cron-Werte, Env-Namen, Routenpfade, Dateinamen exakt.
- **Typ-Konsistenz:** `warnings[]` immer `{ leagueName, playerName, pos, team, severity, reason }`; `composeMessage` → `{ title, body, url, tag } | null`; `runCheck` → `{ checked, sent, pruned }`; Subscription-Form in Task 2/4/6 identisch (`endpoint`, `keys.{p256dh,auth}`, `sleeperUsername`).
